{
  "name": "workspace-demo",
  "version": "1.0.0",
  "description": "Workspace integration example for @dweb-cloud/safenv",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm run --recursive build",
    "start": "pnpm run --recursive start",
    "dev": "pnpm run --recursive dev",
    "validate": "pnpm run --recursive validate",
    "generate-types": "pnpm run --recursive generate-types",
    "clean": "pnpm run --recursive clean",
    "setup": "./scripts/setup.sh",
    "test": "./scripts/test.sh"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "tsdown": "^0.2.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
